Put `rust_seg_unity.onnx` (123 MB) in this folder.

It isn't included in the zip or in GitHub because of its size. Copy it from your
original project (`Dashboard/model/rust_seg_unity.onnx`). On Hugging Face Spaces
it's stored with Git LFS; on other hosts you can set MODEL_URL instead.
